<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="unidad-venta.list">
      <field name="name">unidad-venta list</field>
      <field name="model">unidad-venta.unidad-venta</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="unidad-venta.action_window">
      <field name="name">unidad-venta window</field>
      <field name="res_model">unidad-venta.unidad-venta</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="unidad-venta.action_server">
      <field name="name">unidad-venta server</field>
      <field name="model_id" ref="model_unidad-venta_unidad-venta"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="unidad-venta" id="unidad-venta.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="unidad-venta.menu_1" parent="unidad-venta.menu_root"/>
    <menuitem name="Menu 2" id="unidad-venta.menu_2" parent="unidad-venta.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="unidad-venta.menu_1_list" parent="unidad-venta.menu_1"
              action="unidad-venta.action_window"/>
    <menuitem name="Server to list" id="unidad-venta" parent="unidad-venta.menu_2"
              action="unidad-venta.action_server"/>
    -->
  </data>
</odoo>